[pytest]
# 测试文件所在目录
testpaths = tests

# 用例命名规则：测试文件以 test_ 开头，函数以 test_ 开头
python_files = test_*.py
python_functions = test_*

# 让输出更易读
addopts = -v --strict-markers

# 自定义标记（Day 13 之后会用到）
markers =
    smoke: 冒烟用例
    regression: 回归用例
    slow: 执行较慢的用例
